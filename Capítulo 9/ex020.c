#include <stdio.h>

int main()
{
    int count;

    for(count = 0; count = 50; count + 1)
    {
        printf("%d ", count);
        if(count > 50)
        {
            break;
        }
    }
    putchar('\n');
    return(0);
}